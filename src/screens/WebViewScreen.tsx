import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const WebViewScreen = () => {
  return (
    <SafeAreaView>
      <Text>WebView</Text>
    </SafeAreaView>
  );
};

export default WebViewScreen;
